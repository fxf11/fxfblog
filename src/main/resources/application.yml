server:
  port: 8282
  servlet:
    context-path: /fxfblog

spring:
  datasource:
    #   数据源基本配置
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=UTC&useSSL=false&useUnicode=true&characterEncoding=UTF-8
    type: com.alibaba.druid.pool.DruidDataSource
    #   数据源其他配置-
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
    #    schema:
    #      - classpath:sql/department.sql
    #      - classpath:sql/employee.sql
    initialization-mode: always

#mybatis-plus
mybatis-plus:
  global-config:
    db-config:
      id-type: auto
      field-strategy: not_empty
      #驼峰下划线转换
      column-underline: true
      #逻辑删除配置
      logic-delete-value: 0
      logic-not-delete-value: 1
      db-type: mysql
    refresh: false
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
  mapper-locations: classpath*:mybatis/mapper/*.xml

#配置分页查询
pagehelper:
  pagehelper: Dialect:mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

#阿里云OSS对象存储
aliyun:
  accessKeyId: LTAI4GK7Hmat6SJoAQs9nGNm
  accessKeySecret: FVBFnjgoMu2xAHaKIR9kuhCIeJeGz9
  oss:
    endpoint: http://oss-cn-hangzhou.aliyuncs.com
    bucketName: fxfblog
    urlPrefix: https://fxfblog.oss-cn-hangzhou.aliyuncs.com/

